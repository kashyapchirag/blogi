<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="cont w-full min-h-screen bg-zinc-900 flex flex-col  text-white px-[3rem] py-[3rem] relative">
        <div class="logout flex justify-end absolute right-6 top-7"><a href="/login" class="rounded-md bg-red-500 h-[2.5rem] p-[10px] w-[4rem] flex items-center justify-center cursor-pointer">logout</a></div>
        <h1 class="title text-[2rem]">Hello, <%= name %> 👋</h2>
        <h4 class="tip text-[0.9rem] mb-[1rem]">You can create new posts</h4>

        <form action="/post" method="post">
            <textarea name="content" id="content" class="mb-[0.6rem] resize-none bg-transparent border-2 border-zinc-800 w-1/3 h-[5rem] p-[0.4rem] rounded-[0.7rem]" placeholder="What's in your mind"></textarea>
            <button type="submit" class="rounded-md bg-blue-500 h-[2.5rem] p-[10px] w-[8%] flex items-center justify-center cursor-pointer">Create</button>
        </form>

        <div class="posts mt-[3rem] w-[100%]">
            <div class="heading text-[1.3rem] mb-[0.6rem]">Your posts</div>
            <div class="postcontainer flex gap-[2rem] flex-wrap">

                <% posts.forEach(element => {%>
                    <div class="post bg-zinc-700 min-w-[38%] max-w-[38%] p-3 border-2 border-zinc-600 rounded-[0.7rem]">
    
                        <div class="username text-blue-500">@<%= username %></div>
                        <div class="content text-[0.8rem] mb-[0.5rem]"><%= element.content %></div>
                        <a href="" class="text-blue-400 text-[0.8rem] mr-[0.2rem]">Like</a>
                        <a href="" class="text-zinc-400 text-[0.8rem]">Edit</a>
                    </div>
                    
                <%}); %>
                
            </div>
        </div>
        
    </div>
    
</body>
</html>