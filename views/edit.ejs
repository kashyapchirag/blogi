!<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="cont w-full min-h-screen bg-zinc-900 flex flex-col  text-white px-[3rem] py-[3rem] relative">
        <div class="logout flex justify-end absolute right-6 top-7"><a href="/profile" class="rounded-md bg-red-500 h-[2.5rem] p-[10px] w-[4rem] flex items-center justify-center cursor-pointer">Back</a></div>
        <h1 class="title text-[2rem]">Hello, <%= name %> 👋</h2>
        <h4 class="tip text-[0.9rem] mb-[1rem]">You can edit your post</h4>

        <form action="/edit/<%= id %>" method="post">
            <textarea name="content" id="content" class="mb-[0.6rem] resize-none bg-transparent border-2 border-zinc-800 w-1/3 h-[5rem] p-[0.4rem] rounded-[0.7rem]"><%= content %></textarea>
            <button type="submit" class="rounded-md bg-yellow-500 text-black h-[2.5rem] p-[10px] w-[6%] flex items-center justify-center cursor-pointer">Save</button>
        </form>

    </div>
    
</body>
</html>